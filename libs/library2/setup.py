from setuptools import setup, find_packages

setup(
    name='library2',  # Replace with your library name
    version='0.1.1',
    packages=find_packages(),
    install_requires=[],  # List any external dependencies your library has
)